# Atlas-Plugin 黑名单
# 每行一条正则，匹配完整消息文本（e.msg）
# 命中正则时图鉴两个入口（快捷入口 + 常规查询）均跳过处理：不回复、不拦截，
# 消息继续传递给其他插件（如 miao-plugin），避免图鉴误抢非图鉴类 #命令。
#
# 示例：
#   patterns:
#     - '^#签到'      → 拦截所有 #签到 开头消息
#     - '#抽卡'       → 拦截包含 #抽卡 的消息
patterns: []