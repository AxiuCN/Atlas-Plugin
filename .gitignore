# 依赖
node_modules/

# 配置文件
config/config.yaml
config/alias/
config/blacklist.yaml

# 操作系统临时文件
.DS_Store
Thumbs.db

# 编辑器配置
.vscode/
.idea/

# Claude Code / DeepSeek Harness 工作区
.claude/
.dsh/
CLAUDE.md
AGENTS.md

# 角色攻略数据仓库（git clone 拉取，非子模块，由 #图鉴初始化 / #图鉴更新 末尾同步）
# 只忽略克隆出来的仓库本体，外层目录保留（可放 README 说明）
tool/Character-Codex-Data/Character-Codex-Data/

# 套件临时产物（git 替身的编译产物、渲染样例图等）
test/.test-tmp/
